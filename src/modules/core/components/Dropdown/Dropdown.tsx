import React from 'react'
import { Root, DropdownMenuProps } from '@radix-ui/react-dropdown-menu'

const Dropdown: React.FC<DropdownMenuProps> = props => {
  return <Root {...props} />
}

export default Dropdown
